"""Legacy E2E workflow tests placeholder."""

import pytest

pytest.skip(
    "Legacy StaffTask/Team workflows removed during WorkItem migration. "
    "Integration tests require redesign against the unified WorkItem system.",
    allow_module_level=True,
)
